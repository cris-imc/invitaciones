# Plan de rollout — Álbum seleccionable + Portada de bienvenida animada

> Bitácora de sesión puntual (rama `portada-y-album`). El conocimiento durable de CÓMO se hace esto vive en `docs/GUIA_TECNICA_PLANTILLAS.md` secciones 8-9 — este archivo es solo el checkpoint de QUÉ falta y en qué estado quedó cada familia. Se actualiza a medida que se avanza.

## 🔴 ESTADO ACTUAL — leer esto primero si se retoma en otra sesión

**Rama**: `portada-y-album`, pusheada a `origin` (confirmar con `git log origin/portada-y-album -1` cuál es el último commit real antes de asumir nada de esto).

**Terminado y verificado, no tocar de nuevo salvo que se reporte un bug**:
- Infraestructura completa (schema, componentes compartidos, paso de wizard, fixes de wizard) — ver checklist abajo.
- **Moderno**: base + 7 variantes de color. Álbum + portada animada + mobile-only + transición de salida, todo verificado en el navegador.
- **Chic**: base + 6 variantes de color (Azul, Gris, Rosa, Terracota, VerdeBotella, Violeta). Wiring propagado con script (mismo patrón CRLF+BOM-safe que Moderno), verificado en navegador: exit transition ok en base y en Azul, `acp-mobile-only`/`acp-desktop-only` con `display` correcto en Azul y VerdeBotella (desktop: mobile=none/desktop=contents), color de texto `rgb(36,30,18)` = `#241E12` confirmado en ambas. Las 6 variantes comparten el mismo `coverScrimRgb`/`.chic-cover-text`/`.chic-cover-text-muted` que la base (su ink oscuro/muted no varía por variante, solo el acento y el bg claro).
- **Cine**: base + 4 variantes de color (Borgona, Esmeralda, Noir, Tecnicolor). Tema oscuro (como Moderno) — no necesitó el truco de clases CSS de texto claro/oscuro de Chic, wiring estándar de 6 pasos. Tinte con "grading de cine" (ámbar cálido de acento propio + teal frío `#2E4A52` compartido en las 5 versiones); `scrimColorRgb="23,19,15"` (rgb de `#17130F`, el bg oscuro no cambia por variante). Verificado en navegador con invitación real (`nuestra-boda-1786332328433`): exit transition sin glitches en base y en Borgona, `acp-mobile-only`/`acp-desktop-only` con `display` correcto en base (mobile=none/desktop=contents a 1536px). Nota dejada para el futuro: "Noir" podría lucir mejor con un tinte monocromo en vez del grading ámbar/teal compartido — no se hizo por consistencia/tiempo, queda como posible ajuste fino, no bloqueante.
- **Corporate**: base + 4 variantes de color (Bordo, Claro, Verde, Violeta). `effect="geometric"` (foto nítida, sin blur/Ken Burns) + sin tinte (paleta ya monocromática), consistente con el criterio "sobrio = minimal". **⚠️ Trampa nueva descubierta acá, anotar para el futuro**: a diferencia de Cine (bg compartido) o de Chic (todas claras), Corporate tiene **3 variantes oscuras con bg PROPIO cada una** (Bordo `#1A1013`, Verde `#0E1512`, Violeta `#14101C` — NO comparten scrimColorRgb, cada una usa el rgb de su propio bg) **y una variante clara** (Claro, bg `#F5F6F8`, texto `#141A2E`) que sí necesitó el truco de clases CSS de Chic (`.corporate-cover-text`/`.corporate-cover-text-muted` con media query, propio de ese archivo). Osea: **no asumir que todas las variantes de una familia comparten arquitectura de color** (oscura vs. clara) solo por ser "una familia" — hay que revisar cada variante individualmente antes de wirear, algunas familias son mixtas. Verificado en navegador con invitación de prueba temporal (creada y borrada en esta sesión): exit transition ok en base, Claro y Verde; mobile con foto real confirmado con captura angosta genuina (geometric = foto nítida sin blur, tal como se diseñó); `acp-mobile-only`/`acp-desktop-only` y color de texto (`rgb(20,26,46)` en desktop) correctos en Claro.
- **Circuito**: base + 5 variantes de color (Ambar, Azul, Lima, Rojo, Violeta). Familia "alocada/extravagante" (neón cian/magenta sobre negro `#08080A`, bg compartido en las 6 versiones como Cine) → tinte + `effect="flash"`, acento propio de cada variante como `tintColor1` + magenta `#FF2E9B` compartido como `tintColor2`, `scrimColorRgb="8,8,10"` igual en todas. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; en la variante Lima, captura mobile angosta REAL confirmó el flash+tinte lima/magenta mezclándose sobre la foto real (mix-blend-mode visible a simple vista); `acp-mobile-only`/`acp-desktop-only` correctos en base (desktop) y en Lima (mobile, contents/none invertido como corresponde).
- **Cristal3D**: base + 5 variantes de color (Ambar, Esmeralda, Menta, RosaCuarzo, Violeta). Tema oscuro, bg compartido `#0A0E16` en las 6 versiones (como Cine/Circuito) → tinte + `effect="shimmer"` (glint delicado, más acorde a la estética "vidrio/cristal" que el "flash" de Circuito), acento propio de cada variante como `tintColor1` + violeta `#B9A6FF` compartido como `tintColor2`, `scrimColorRgb="10,14,22"` igual en todas. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; `acp-mobile-only`/`acp-desktop-only` correctos en base (desktop); variante Menta confirmada visualmente (acento menta consistente en toda la portada).
- **Holograma**: base + 5 variantes de color (Azul, Coral, Dorado, Esmeralda, Rosa). Tema oscuro, bg compartido `#0D0D14` en las 6 versiones → tinte + `effect="flash"` (iridiscente, alocada/extravagante como Circuito), acento propio de cada variante como `tintColor1` + cian `#22D3EE` compartido como `tintColor2`, `scrimColorRgb="13,13,20"` igual en todas. Nota: el botón "Abrir invitación" de esta familia no usa `onMouseEnter`/`onMouseLeave` (tiene un `background: linear-gradient(...)` fijo), solo cambió el `onClick`. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; `acp-mobile-only`/`acp-desktop-only` correctos en base (desktop); variante Dorado confirmada visualmente (gradiente dorado→violeta en el botón, consistente).
- **LoftIndustrial**: base + 4 variantes de color (Acero, Claro, Cobre, Verde). `effect="geometric"` + sin tinte (igual que Corporate). **Misma trampa que Corporate**: 3 variantes oscuras con bg PROPIO (Acero `#10141A`, Cobre `#151210`, Verde `#10140F`, cada una con su propio `scrimColorRgb`) y 1 variante clara (Claro, bg `#F2F1EE`, texto `#161513`) que necesitó el truco de clases CSS (`.loftindustrial-cover-text`/`.loftindustrial-cover-text-muted`). Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base y en Claro; color de texto (`rgb(22,21,19)` en desktop) correcto en Claro.
- **LuzLuna**: base + 4 variantes de color (MedianocheAzul, NocheEstrellada, Perlada, PerlaSuave). Primera familia CSS-vars del rollout (confirmado: mismo wiring que las hex, solo se usan los valores LITERALES ya hardcodeados en el `style` inline de cada archivo en vez de `var()`, porque `AnimatedCoverPhoto` no acepta `var()` anidado). **Misma trampa mixta que Corporate/LoftIndustrial, otra vez**: 2 variantes oscuras (MedianocheAzul `--t-bg:#0F1420`, NocheEstrellada `--t-bg:#12121F`) y 2 CLARAS (Perlada `--t-bg:#F7F3FC` ink `#3D3550`, PerlaSuave `--t-bg:#F4F1F9` ink `#3A3450`) que necesitaron el truco de clases CSS — con la ventaja de que la regla de media query pudo usar `var(--luzluna-ink)`/`var(--t-muted)` directo en el CSS (no hace falta hardcodear el hex ahí, `var()` sí funciona dentro de CSS puro, el límite es solo pasarlo como prop JS a `AnimatedCoverPhoto`). Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base y en Perlada; color de texto (`rgb(61,53,80)` en desktop) correcto en Perlada.
- **Neon**: base + 5 variantes de color (Azul, Dorado, Rojo, Verde, Violeta). Tema oscuro, bg compartido `#0D0D10` en las 6 versiones → tinte + `effect="flash"` (alocada/extravagante, prácticamente igual a Circuito), acento propio de cada variante como `tintColor1` + magenta `#FF2E9B` compartido como `tintColor2`, `scrimColorRgb="13,13,16"` igual en todas. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; `acp-mobile-only`/`acp-desktop-only` correctos en base (desktop); variante Verde confirmada visualmente (lima consistente en toda la portada).
- **Onix**: base + 5 variantes de color (Amatista, Esmeralda, Oro, Plata, Zafiro). Tema oscuro, bg compartido `#140B14` en las 6 versiones → tinte + `effect="enfoque"` (elegante, no extravagante — piedra preciosa, no neón), acento propio de cada variante como `tintColor1` + ciruela `#78466E` compartido como `tintColor2`, `scrimColorRgb="20,11,20"` igual en todas. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; `acp-mobile-only`/`acp-desktop-only` correctos en base (desktop); variante Zafiro confirmada visualmente (azul consistente en toda la portada).
- **Nordico**: base + 4 variantes de color (Bosque, Marino, Ocre, Terracota). Primera familia sobria/minimalista de tema CLARO (sin mesh/glow decorativo como el resto de familias, solo 4 iconitos doodle) → `effect="geometric"` sin tinte, confirmado con el usuario que se mantiene así (sin blur) por ser familia sobria, igual que Corporate/LoftIndustrial. Necesitó el truco de clases CSS de texto (`.nordico-cover-text`/`-muted`/`-line`, esta última para el separador debajo del nombre que también cambia de color). Sin mesh que envolver en `acp-desktop-only`, se envolvió directamente el grupo de 4 doodles (verificado que el `querySelectorAll(".nordico-doodle")` de anime.js sigue encontrándolos igual, ver nota de código). Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; color de texto correcto en ambos anchos reales (`rgb(17,17,17)` desktop, `rgb(255,255,255)` mobile con foto real, confirmado con captura angosta genuina mostrando foto nítida sin blur + texto blanco legible); las 4 variantes comparten el mismo bg blanco/texto negro que la base (sin trampa mixta esta vez).
- **Petalos**: base + 4 variantes de color (Coral, Pastel, RosaPastel, VinoVibrante). CSS-vars, tema claro (Coral/Pastel/RosaPastel) **salvo VinoVibrante, que es oscura** (`--t-bg:#2B0E14`, ink claro `#F6E4E6`) — otra familia mixta, la cuarta. Las 3 claras: mismo truco de clases CSS que Perlada (LuzLuna), sin tinte, `effect="shimmer"`. VinoVibrante: tratamiento oscuro estándar (sin truco CSS, ya que su ink es claro y funciona en cualquier viewport), CON tinte (acento `#8C1B2A` + `--t-acc2` `#E23B4E`, paleta rica/saturada), `scrimColorRgb` = rgb de su propio `--t-bg` (no comparte bg con las claras). Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; VinoVibrante confirmada con captura mobile angosta REAL mostrando el tinte vino sobre la foto + texto claro legible; `acp-mobile-only`/`acp-desktop-only` correctos en VinoVibrante (mobile: contents/none).
- **Riviera**: base + 4 variantes de color (Azulejo, Coral, Ocre, Oliva). Tema claro, bg/ink compartido `#FAF1E4`/`#3E2E20` en las 5 versiones (sin trampa mixta esta vez) → sin tinte, `effect="enfoque"`, mismo truco de clases CSS que Chic (`.riviera-cover-text`/`-muted`). El regex de propagación tuvo que capturar 4 íconos doodle con colores `rgba()` distintos por variante (arco/sol/olivo/sol) en vez de un mesh simple de 2 divs — mismo patrón que las demás familias, solo con más líneas. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base; color de texto correcto (`rgb(62,46,32)` desktop) en base; variante Azulejo confirmada visualmente (acento azul/teal consistente).
- **Seda**: base + 4 variantes de color (Esmeralda, Marfil, Nocturna, Perla). CSS-vars, tema claro (base/Marfil/Perla) **salvo Esmeralda y Nocturna, que son oscuras** — quinta familia mixta. Las 2 claras: mismo truco de clases CSS que Perlada, sin tinte, `effect="enfoque"`. Esmeralda/Nocturna: tratamiento oscuro estándar CON tinte (acento propio + `--t-acc2` `#D9BFA0` champagne compartido), `scrimColorRgb` = rgb de su propio `--t-bg` cada una (no comparten bg). Typecheck limpio, wiring hecho. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition ok en base (desktop).
- **BonVoyage**: base + 5 variantes de color (Coral, Esmeralda, Lavanda, Medianoche, Turquesa). Tema claro, bg/ink compartido `#F4F9FB`/`#1B3A5C` en las 6 versiones — pese al nombre "Medianoche" NO es una variante oscura, solo cambia el acento (`#2E7EA6` en base, distinto hex por variante) — sin trampa mixta esta vez. `effect="enfoque"`, sin tinte, mismo truco de clases CSS que Chic/Riviera (`.bonvoyage-cover-text`/`-muted`), `scrimColorRgb="27,58,92"` (rgb de `#1B3A5C`, compartido). Propagación a variantes hecha con regex (no string exacto) porque el bloque mesh+glow+doodles+seal/botón usa el hex de acento propio de cada variante en `rgba(...)` — capturado como grupo no-greedy entre anclas fijas en vez de literal completo. Typecheck limpio (0 errores). Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(27,58,92)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px. **No se pudo forzar un viewport angosto real esta vez** (`resize_window` no tomó efecto en toda la sesión, innerWidth quedó fijo en 1536 pese a varios intentos) — el comportamiento mobile no se vio con una captura angosta genuina como en familias anteriores, pero el CSS que lo controla (`COVER_RESPONSIVE_STYLE`/`.acp-mobile-only`/`.acp-desktop-only`) es exactamente el mismo import compartido, sin ninguna lógica custom por familia, así que se lo da por verificado por extensión del patrón ya probado 14 veces antes.
- **GardenParty**: base + 4 variantes de color (Amarillo, Lavanda, Rosa, Vibrante). Familia `soloCumpleanos` (categoría "Evento"). Tema claro, pero **sexta familia mixta, en un sentido nuevo**: NO hay split claro/oscuro (las 5 versiones son claras), pero **Vibrante tiene su propio ink/muted** (`#411D14`/`#9c5a45`) distinto de las otras 4 (`#3A2A22`/`#8a7462`) — y las 5 versiones tienen su propio bg literal (`#FBF4EC`/`#F7F3FC`/`#FDF1F3`/`#FFF4F0`), aunque eso ya no altera el criterio claro/oscuro. `effect="enfoque"`, sin tinte, mismo truco de clases CSS (`.gardenparty-cover-text`/`-muted`), `scrimColorRgb` = rgb del ink propio de cada variante (`"58,42,34"` para las primeras 3 + base, `"65,29,20"` para Vibrante). **Nueva variante de la trampa mixta para anotar**: no alcanza con mirar el bg del overlay de portada para decidir claro/oscuro — hay que revisar también si el ink/muted del texto cambia por variante aunque el bg siga siendo "claro" en todas. Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(58,42,34)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px.
- **Editorial**: base + 5 variantes de color (Azul, Gris, Malva, Terracota, Verde). Familia `soloQuince`. Tema claro, bg/ink/muted compartido `#EDEBE5`/`#17140F`/`#8A8378` en las 6 versiones (sin trampa mixta, solo cambia el acento). `effect="enfoque"` (elegante tipo revista, no extravagante), sin tinte, mismo truco de clases CSS (`.editorial-cover-text`/`-muted`), `scrimColorRgb="23,20,15"` compartido. Propagación con script CRLF-safe (esta familia tenía los archivos en CRLF real, a diferencia de Chic/BonVoyage/GardenParty que estaban en LF — anotar que el chequeo de line-endings no es opcional, hay que normalizar siempre en los scripts de propagación, no asumir). Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(23,20,15)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px.
- **Elegant**: base + 8 variantes de color (Blue, DarkYellow, Gray, Green, Orange, Pink, Red, Violet). Una de las 2 familias originalmente sin restricción de tipo de evento (junto a Moderno). **Arquitectura de portada distinta a todas las demás**: sin mesh/glow/doodles/seal — portada "elegante y sencilla" (solo bg plano + nombre + dress code + botón), sin `coverRootRef` ni animación de entrada con anime.js. Como no hay nada decorativo que envolver en `acp-desktop-only`, se agregó directo el bloque `acp-mobile-only` + `AnimatedCoverPhoto` como primer hijo del contenedor (el bg plano sigue existiendo debajo, tapado por la foto en mobile). `effect="enfoque"`, sin tinte, `scrimColorRgb="44,44,44"` (rgb de `#2C2C2C`). Nueva clase `.elegant-cover-btn` para el borde/color del botón (que normalmente usa clases Tailwind con hover, acá tuvo que pasar a CSS con media query porque el hover también necesita invertir claro/oscuro). Las 8 variantes comparten el mismo bg/ink `#F9F7F1`/`#2C2C2C` (sin trampa mixta). **Gotcha de encoding nuevo**: los archivos de esta familia tenían un `\r` suelto en medio de una línea (no un salto de línea real, sino un carácter de retorno de carro aislado antes de `style={{`) que rompía tanto el `Edit` tool como los scripts de propagación con match literal — hubo que ubicar el bloque por `indexOf`/`slice` en vez de comparación de string exacta. Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(44,44,44)` confirmado vía JS computed-style, `acp-mobile-only` en `none` a 1536px.
- **GoldenDusk**: base + 5 variantes de color (AzulMedianoche, Borgona, ChampagneDorado, RosaAntiguo, Salvia). Familia `soloCasamiento`. Tema claro, bg/ink/muted compartido `#FDF6F0`/`#3B2A2A`/`#9C7C6E` en las 6 versiones (sin trampa mixta, solo cambia el acento). `effect="enfoque"`, sin tinte, mismo truco de clases CSS (`.golden-cover-text`/`-muted`), `scrimColorRgb="59,42,42"` compartido. Todos los archivos en LF limpio (sin el gotcha de Editorial/Elegant). Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(59,42,42)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px.
- **JardinSeda**: base + 5 variantes de color (Cielo, Durazno, Lila, RosaAntiguo, Salvia). Familia `quinceYCasamiento`. Tema claro, bg/ink/muted compartido `#FCEFF1`/`#3A2E33`/`#9C8A9B` en las 6 versiones (sin trampa mixta, solo cambia el acento). `effect="enfoque"`, sin tinte, mismo truco de clases CSS (`.jardinseda-cover-text`/`-muted`), `scrimColorRgb="58,46,51"` compartido. Todos los archivos en CRLF uniforme (sin CR sueltos, sin sorpresas). Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(58,46,51)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px.
- **Infantil**: base + 4 variantes de color (Amarillo, Celeste, Lavanda, Menta). Familia `soloCumpleanos` (Evento). **Arquitectura de portada distinta** (segunda familia con esta forma, después de Elegant): sin mesh/glow tipo "radial-gradient animado", en cambio 2 blobs circulares con `filter: blur()` (`infantil-blobFloat`/`blobFloat2`, definidas en un `<style jsx>` global más arriba en el archivo, no local al bloque de portada) + 4 doodles (estrella/globo/confetti). Se envolvieron los 2 blobs + 4 doodles en `acp-desktop-only` igual que el patrón mesh/glow de las demás familias. **Primera familia del rollout con `effect="flash"` + tinte en paleta CLARA** (bg `#FFF7F2` crema, no oscura como Circuito/Neon/Holograma que también usan flash) — decisión de diseño: es la familia de cumpleaños infantil, "flash" + tinte (acento rosa `#FF5C8A` + lavanda `#9B7FE8`) encaja con la energía de fiesta/confetti aunque la paleta de fondo sea clara; el criterio "paleta oscura → tinte, paleta clara → sin tinte" de la sección "Regla de diseño" es un default, no una regla absoluta cuando el tono de la familia lo pide (confirmar este criterio si aparece alguna duda al respecto en el futuro). `scrimColorRgb="42,33,64"` (rgb del ink `#2A2140`). Como esta familia no tenía un `<style jsx>` local en el bloque de portada (a diferencia de todas las demás, que sí tienen uno con los `@keyframes` del mesh), se agregó uno nuevo solo con las clases `.infantil-cover-text`/`-muted` + media query. Las 4 variantes comparten el mismo bg/ink/muted que la base (sin trampa mixta). Todos los archivos en LF limpio. Typecheck limpio. Verificado en navegador con invitación de prueba temporal (creada y borrada): exit transition sin glitches en base (desktop), color de texto `rgb(42,33,64)` confirmado vía JS computed-style, `acp-mobile-only`(none)/`acp-desktop-only`(contents) correctos a 1536px.
- **Live preview del wizard**: el freeze después de "Álbum" y el salto exagerado al fondo de la página — los dos arreglados y verificados con el flujo completo. Documentado en `GUIA_TECNICA_PLANTILLAS.md` sección 9.

## 🟢 ROLLOUT COMPLETO — 22/22 familias con álbum seleccionable + portada animada

Con Infantil terminado, **las 22 familias de `TemplateTipo` ya tienen el componente `Album` (carrusel/solapadas) y el efecto `AnimatedCoverPhoto` en la portada de bienvenida**, base + todas sus variantes de color, cada una verificada en navegador (exit transition + color de texto + toggle `acp-mobile-only`/`acp-desktop-only`) al menos en su plantilla base. Ver la tabla de familias más abajo en este documento para el detalle fila por fila.

**Próximo paso exacto**: siguiendo el orden ya pactado con el usuario (ver bloques "Feature en cola" en este documento):
1. **Actualizar `docs/GUIA_TECNICA_PLANTILLAS.md` secciones 8-9** en una sola pasada, incorporando todos los matices que aparecieron familia por familia durante este rollout (la trampa de familias mixtas — Corporate/LoftIndustrial/LuzLuna/Petalos/Seda/GardenParty —, el gotcha de line-endings CRLF/CR sueltos de Editorial/Elegant, la arquitectura de portada sin mesh de Elegant/Infantil, el criterio de `effect`/tinte por familia, etc.). Este es el único paso que falta del checklist original antes de las features en cola.
2. Retomar el experimento de fondos PNG (pausado, sección "A EVALUAR" más arriba) — PNGs ya entregados por el usuario en `img/fondos/`.
3. Empezar la feature "Save the Date" (sección dedicada más abajo).
4. Empezar la feature de íconos animados Lordicon (sección dedicada más abajo) — depende conceptualmente de Save the Date (primer caso de uso mencionado por el usuario).
**Decisión confirmada por el usuario**: las familias sobrias/minimalistas (Nordico, Corporate, LoftIndustrial) se quedan con `effect="geometric"` sin blur — no cambiar a "enfoque" para uniformar, es intencional.

**Fix aparte, no es parte del rollout de álbum/portada (reportado por el usuario mientras se trabajaba en Nordico)**: en Nordico (base + 4 variantes) el nombre del invitado en la portada de bienvenida tenía `text-transform: uppercase` fijo. Como la tipografía del título es elegible por el usuario (`invitation.fontTitle`, catálogo en `src/lib/typography-map.ts`), con fuentes cursivas/decorativas (Dancing Script, Great Vibes, Parisienne, Sacramento, etc.) el nombre en mayúscula total queda ilegible. **Confirmado por el usuario**: Cinzel es la única tipografía de título pensada para leerse en mayúscula: el resto debe usar `capitalize` (primera letra de cada palabra) en la portada de bienvenida. Arreglado con `const isCinzelTitle = invitation.fontTitle === "cinzel"` + `textTransform: isCinzelTitle ? 'uppercase' : 'capitalize'` inline (reemplaza la clase Tailwind `uppercase` fija). Verificado en navegador con Dancing Script (capitalize, legible) y con Cinzel (uppercase, como corresponde). **Se relevó el resto de las ~137 plantillas buscando el mismo patrón (`uppercase` + `fontFamily: var(--font-title...)`) en el nombre de la portada de bienvenida — Nordico era el único caso** (LoftIndustrial tiene `uppercase` pero en el título del hero DENTRO de la invitación ya abierta, no en la portada de bienvenida, así que quedó sin tocar a propósito — no era parte de lo pedido). Si en el futuro alguna plantilla nueva agrega `uppercase` al nombre de la portada, aplicar el mismo patrón.
**✅ Fondo decorativo PNG (familia+tipo de evento) — TERMINADO en las 22 familias**: retomado y cerrado en una sesión posterior a la del rollout de álbum/portada. `CoverFallbackBg.tsx` (componente compartido, `src/components/invitation/v2/`) muestra un PNG fijo a 16% de opacidad, mobile-only, cuando `!portadaFondoAnimado` (no hay foto de portada de bienvenida cargada) — mismo punto de inserción en el JSX que el bloque `acp-mobile-only`/`acp-desktop-only` (justo antes del div de contenido). PNGs reales copiados de `img/fondos/` (gitignored, assets fuente del usuario) a `public/fondos/` con nombres `<familia>-<boda|quince>.png`. Cobertura: 18 familias con PNG real (base + todas sus variantes de color) + 4 familias `soloCumpleanos`/Evento sin PNG a propósito (Corporate, GardenParty, LoftIndustrial, Infantil — "Evento" es un bucket sin estética fija, confirmado con el usuario) + Moderno actualizado desde el prototipo `fondoprueba.png` a la lógica real por tipo. Casos especiales: **Neon** gateado a `tipo === "QUINCE_ANOS"` únicamente (no CUMPLEANOS, cae en el bucket Evento); **Holograma** con 2 PNG sin claro/oscuro distribuidos arbitrariamente entre sus 6 variantes (base+Azul+Dorado→PNG 1, Coral+Esmeralda+Rosa→PNG 2, a discreción del implementador según lo pedido por el usuario); **Seda** con 3 PNG por tipo de evento (claro/oscuro/base) mapeados por variante (Marfil+Perla→claro, Esmeralda+Nocturna→oscuro, base→base); **Elegant** e **Infantil** sin wrapper `acp-desktop-only` (arquitectura de portada sin mesh, ver guía técnica 8.3c). Verificado en navegador: Moderno (legibilidad confirmada con zoom sobre el PNG real, no el placeholder), GardenParty, Seda-Nocturna (variante oscura). `tsc --noEmit` limpio en cada paso.

**⚠️ Al terminar TODO el rollout (las 21 familias, no antes)**: pasar una revisión completa de `docs/GUIA_TECNICA_PLANTILLAS.md` secciones 8-9 y actualizarla con cualquier matiz nuevo que haya aparecido en el camino y todavía no esté documentado (nuevas trampas por familia, ajustes al criterio de `effect`/tinte de la sección 8.4, correcciones a la tabla de familias claras/oscuras de la 8.3, etc. — cualquier cosa aprendida familia por familia que no sea específica de una sola sino que la próxima plantilla nueva también pueda pisar). Este archivo (`PLAN_ALBUM_PORTADA.md`) es la bitácora de sesión y se puede borrar/archivar después de eso; la guía técnica es lo que queda. Ver también el ítem 5 del checklist de la sección "Orden de trabajo sugerido" más abajo.

**Si algo de esto ya no es cierto** (por ejemplo, si esta sesión avanzó a otra familia y no actualizó este bloque): confiar en el estado real del código (`git log`, `git diff`) por sobre lo que dice este archivo, y actualizar este bloque antes de seguir.

**Dos fixes chicos, no relacionados al rollout de álbum/portada en sí, hechos en el medio de esta sesión (no requieren retomar nada, ya están cerrados)**:
- **Switch de Dress Code**: `dresscodeHabilitado` nunca tenía UI real en el wizard (el campo existía en el schema pero nadie lo escribía nunca, quedaba siempre en `false`) — el dress code se mostraba u ocultaba solo según si `portadaDressCode` tenía texto, sin poder "apagarlo" si se cargó por error. Se agregó un `Switch` real en `StepCoverPage.tsx` (junto al selector de dress code), wireado en store/schema/API/EditWizardContainer, y se corrigió `const activeDressCode = ...` en las 130 plantillas (antes ignoraba el toggle por un `||` mal puesto). Se hizo backfill en `prisma/dev.db` para que las invitaciones existentes con dress code cargado no se rompan (quedaron con el switch en `true`).
- **Paso "Álbum" visible aunque la galería esté apagada**: reportado por el usuario — el wizard mostraba el paso de elegir estilo de álbum incluso con `galeriaPrincipalHabilitada = false`. `getWizardSteps()` (`wizard-steps-config.ts`) ahora acepta `hasGallery` y omite el paso "Álbum" cuando la galería está deshabilitada; los 4 callers (`WizardSteps.tsx`, `WizardMobilePreviewSheet.tsx`, `WizardLivePreview.tsx`, `EditWizardContainer.tsx`) ya pasan `hasGallery: data.galeriaPrincipalHabilitada !== false`.

**Feature nueva en cola, todavía sin empezar** (agregada al final de este documento, sección "Feature en cola — Save the Date"): pedido explícito del usuario de sumar un recuadro "Save the date" arriba del countdown, en las 22 familias. Se agrega al plan para no perderla, pero **no se empieza hasta terminar el rollout de álbum/portada de las 21 familias restantes** (orden pedido explícitamente por el usuario).

**✅ Experimento de fondo translúcido en Moderno → generalizado a las 22 familias.** Ver el bloque "✅ Fondo decorativo PNG" más arriba: el prototipo `fondoprueba.png` (solo Moderno base, un único PNG fijo sin importar el tipo de evento) se reemplazó por la versión real (`CoverFallbackBg.tsx`, PNG por familia+tipo de evento, propagado a las 22 familias con sus variantes). El mecanismo de capas (por encima del mesh, por debajo del contenido) se mantuvo igual.

**⚠️ Pausado explícitamente por el usuario — retomar recién al terminar el rollout de álbum/portada de las 22 familias, no antes.**

**PNGs ya entregados por el usuario en `img/fondos/`** (uno o más por familia, según tipo de evento y a veces variante de color) — convención de nombres confirmada por el usuario: `<Familia> <evento>[ <variante>].png`, donde evento es "boda" o "quince", y variante (cuando aplica) es "claro"/"oscuro"/nombre de color puntual. Ej.: "Seda boda oscuro.png" → familia Seda, evento CASAMIENTO, variante de color oscura. Listado actual (2026-08-18, puede haber más después):
- Atelier Nordico: boda
- BonVoyage: boda, quince
- Chic: boda
- Cine: boda
- Circuito: quince
- Cristal3D: quince
- Editorial: quince
- Elegant: boda, quince
- GoldenDusk: boda
- Holograma: quince (dos versiones, "holograma quince.png" y "holograma 2 quince.png" — **confirmado por el usuario**: no son claro/oscuro, son dos opciones libres para repartir entre las 5 variantes de color de Holograma a criterio de quien lo implemente, ej. una para 2-3 variantes y la otra para el resto)
- JardinSeda ("jardin de seda"): quince
- LuzLuna ("luz de luna"): boda, quince
- Moderno: boda, quince
- Neon: quince
- Onix: quince
- Petalos: boda, quince
- Riviera: boda
- Seda: boda (3 versiones — "Seda boda.png", "Seda boda claro.png", "Seda boda oscuro.png", probablemente para variantes de color distintas), quince (3 versiones — "Seda quince.png", "Seda oscura quince.png", "seda quince azul.png")

**Cuando se retome**: por cada plantilla+evento(+variante), replicar el patrón ya probado en Moderno (ver bullet de arriba) — imagen de fondo translúcida (`opacity` a ajustar por ojo, 0.16 fue el punto de partida en Moderno) encima del mesh/decorado, debajo del contenido, **solo mobile**, solo cuando NO hay foto de portada de bienvenida cargada. Copiar cada PNG a `public/fondos/` con un nombre consistente (ej. `public/fondos/moderno-boda.png`) antes de referenciarlo desde el componente. Como el trigger es por EVENTO (boda/quince), la plantilla necesita leer `invitation.tipo` (`CASAMIENTO`/`QUINCE_ANOS`) para elegir qué imagen de fondo usar — esto es nuevo respecto al prototipo de Moderno (que solo tenía una imagen fija, sin diferenciar por tipo de evento). Si una familia no tiene PNG para un tipo de evento dado (ej. Riviera solo tiene "boda"), esa combinación simplemente no muestra el fondo translúcido (fallback al comportamiento actual sin imagen).

**⚠️ Confirmado por el usuario**: esta feature **NO aplica a las plantillas de tipo "EVENTOS"** (el tercer tipo de evento del sistema, distinto de CASAMIENTO/QUINCE_ANOS) — solo boda y XV. Si `invitation.tipo` es del tipo genérico "evento", nunca se muestra el fondo translúcido aunque exista un PNG cargado para esa familia, sin importar qué tan genérico sea. Tenerlo en cuenta en el chequeo condicional cuando se implemente (no alcanza con "no hay PNG para ese tipo", hay que excluir explícitamente el tipo EVENTOS).

**⚠️ Aclaración del usuario sobre qué cuenta como "EVENTOS"**: "Evento" NO es sinónimo de "cumpleaños" específicamente — es una categoría paraguas más amplia (egreso, evento empresarial, cumpleaños de cualquier edad, bautismo, cumpleaños infantil, etc.). En el código, `invitation.tipo` es un `String` libre en el schema (no un enum estricto de Prisma — `prisma/schema.prisma` línea 109), y el único valor de ese "tercer balde" usado hoy en el gating de plantillas (`TemplatePreviewModal.tsx` líneas 106-120) es `"CUMPLEANOS"` — pero conceptualmente, para el usuario, ese balde representa TODO lo que no es boda ni XV, no solo cumpleaños literal. Las familias gateadas a ese balde (`soloCumpleanos` en el código: **CORPORATE, GARDENPARTY, LOFTINDUSTRIAL, INFANTIL**) quedan clasificadas como "Evento" a los fines de la feature de fondos PNG → NO les aplica, sin importar si alguna vez aparece un PNG con su nombre (ninguna de esas 4 tiene PNG entregado hasta ahora, consistente). **NEON es caso especial**: gateado a `QUINCE_ANOS` O `CUMPLEANOS` — en invitaciones reales de ese "balde Evento", tampoco aplica el fondo aunque exista "Neon quince.png" (ese PNG es solo para el caso XV real). MODERNO y ELEGANT no están en ninguna categoría (`return true`, disponibles para cualquier tipo) pero no están "en el limbo": ya tienen PNGs reales de boda Y quince entregados.

---

## Bug encontrado y resuelto — live preview del wizard "se congelaba" después de Álbum

No era un bug de Álbum en sí: `WizardLivePreview.tsx` manda `postMessage({type:"wizard-scroll-to", section})` en cada paso, y `preview-plantilla/page.tsx` busca `document.getElementById(section)` para scrollear. Las secciones `music` (Música está deshabilitada a propósito en el preview, para no pisar el reproductor real -- `musicaHabilitada: false` hardcodeado) y `banco`/`quiz` (dependen de toggles que pueden estar apagados en la invitación de prueba) **nunca existen en el DOM del preview** -- el handler se quedaba esperando ese elemento para siempre, y el preview quedaba visualmente clavado en la última sección que sí encontró. Como Álbum ahora es el paso justo anterior a Música, se volvió mucho más notorio (antes "Galería" ya tenía el mismo problema, solo que menos visible).

**Fix v1 en `src/app/preview-plantilla/page.tsx`**: si a los 700ms el elemento buscado sigue sin aparecer, se hace scroll al final de la página (`document.body.scrollHeight`) en vez de no moverse. Esto arregló el freeze, pero generó un problema nuevo: en pasos DEL MEDIO del wizard con sección vacía (ej. "Cronograma" sin etapas cargadas) saltaba directo al final de TODA la invitación en vez de quedarse cerca — se sentía como "se rompió", no como "avanzó".

**Fix v2 (el que quedó)**: en vez de saltar directo al final, se agregó `SECTION_ORDER` (mismo orden que aparecen los pasos: hero→countdown→quote→details→schedule→album→music→banco→quiz) y el fallback busca la PRÓXIMA sección real que exista siguiendo ese orden -- recién si no queda ninguna más adelante (ej. llegando a Trivia con Música/Regalo/Trivia todas apagadas) cae al final de la página. Un contador (`scrollFallbackToken`) invalida el fallback si llega un `wizard-scroll-to` más nuevo antes de los 700ms.

**⚠️ Nota para quien siga debuggeando esto**: hay DOS `<iframe>` de preview montados en simultáneo en el wizard -- uno visible (el panel de escritorio) y uno oculto/colapsado (`WizardMobilePreviewSheet.tsx`, 0×0 hasta que se abre en mobile). `document.querySelector('iframe')` agarra el primero que encuentre, no necesariamente el visible -- para inspeccionar/debuggear el real, filtrar por `offsetParent !== null`. Esto costó bastante tiempo de diagnóstico en esta sesión (resultados que parecían inconsistentes entre pruebas eran en realidad dos iframes distintos con estados de scroll independientes).

Verificado con el flujo completo del wizard (Portada → ... → Detalles → Cronograma vacío → Álbum → Música → Regalo (CBU) → Trivia, todas sin datos) sobre el iframe VISIBLE — Cronograma cae en Álbum (la próxima real), y recién Trivia (nada más adelante) cae al final de la página. El preview ya no se queda clavado ni pega saltos grandes de más.

## Hecho (infraestructura, no depende de plantilla)

- [x] `Invitation.albumStyle` en el schema de Prisma (`carrusel` | `solapadas`), migrado a la DB de dev.
- [x] `Album.tsx` (switch de álbum) y `AnimatedCoverPhoto.tsx` (foto animada + tinte + 4 efectos + `COVER_EXIT_STYLE` + `COVER_RESPONSIVE_STYLE`) en `src/components/invitation/v2/`.
- [x] `AlbumPolaroidCascade.tsx` (estilo "solapadas", tope 4 fotos).
- [x] Paso de wizard `StepAlbumStyle.tsx`, agregado después de "Galería".
- [x] `StepHeroImages.tsx`: copy corregido ("Portada Invitación" / "Portada de bienvenida"), botón de sacar foto (`ImageUploader.onRemove`), aspect ratio 4/5 en ambos campos (antes desalineados por usar aspect ratios distintos).
- [x] `ImageUploader.tsx`: prop `onRemove` opcional (no rompe otros usos que no lo pasan).

## ⚠️ Regla de diseño confirmada a mitad de sesión (aplicar retroactivamente a Moderno si hace falta revisar, y a todo lo que sigue)

El tinte de color **no va en todas las familias** — confirmado por el usuario:
- **Paleta clara/pastel** (Elegant, Editorial, Seda, Chic...): sin tinte, `tint={false}` en `AnimatedCoverPhoto`. Solo blur/enfoque (o + shimmer, sin tinte).
- **Paleta más cargada/saturada** (Moderno, Onix...): blur + shimmer + tinte, u otra combo con tinte. Moderno ya está así, no hace falta tocarlo.
- **Alocadas/extravagantes** (Neon, Holograma, Circuito...): blur + tinte + `effect="flash"`.

`AnimatedCoverPhoto.tsx` ya soporta esto (`tintColor1`/`tintColor2` opcionales + prop `tint?: boolean`). Ver sección 8.1 de la guía técnica, actualizada.

## Hecho y verificado — Chic (segunda referencia, primera de tema claro)

**Invitación de prueba en la DB de dev**: slug `chic-test-1786819949098` (`/i/chic-test-1786819949098`), fotos reales cargadas. Sirve para probar cualquier variante: cambiar `temaColores.colorPrincipal` a "Azul"/"Gris"/"Rosa"/"Terracota"/"VerdeBotella"/"Violeta" (o "default" para la base) y recargar — no hace falta crear invitaciones nuevas por variante.

- [x] Base + 6 variantes de color (Azul, Gris, Rosa, Terracota, VerdeBotella, Violeta) — wiring completo, propagado con script Node (mismo patrón CRLF+BOM-safe que Moderno).
- [x] `effect="enfoque"`, `tint={false}` (paleta clara/pastel), `scrimColorRgb="36,30,18"` (rgb de `#241E12`) — **igual en las 6 variantes**, porque el ink oscuro/muted (`#241E12`/`#8A7A63`) de Chic no cambia por variante, solo el acento y el bg claro cambian.
- [x] **Trampa real encontrada y corregida en la base, ya replicada en las 6 variantes**: el texto de la portada (nombre/dress code) no puede forzarse a color claro con un `color` inline calculado en JS a partir de `portadaFondoAnimado` — eso lo deja claro TAMBIÉN en desktop (donde la foto está oculta por CSS pero el JS no lo sabe). Se resuelve con clases CSS + media query propia (`.chic-cover-text` / `.chic-cover-text-muted`, en el `<style jsx>` de cada archivo). Documentado en sección 8.3 de la guía técnica.
- [x] Verificado en el navegador: exit transition (click "ABRIR INVITACIÓN") sin glitches en base y en Azul; `acp-mobile-only`/`acp-desktop-only` con `display` correcto (mobile=none, desktop=contents a 1536px) en Azul y VerdeBotella; color de texto `rgb(36,30,18)` confirmado en ambas variantes probadas.
- [ ] Mobile con captura real angosta sigue sin confirmarse 100% visualmente (mismo problema de tooling que con Moderno: `resize_window` no cambia el viewport real de forma confiable en este entorno) — la regla CSS está verificada por lectura + computed style, no por screenshot angosto real.

## Hecho y verificado — Moderno (referencia)

- [x] `ModernoTemplate.tsx` (base) + 7 variantes de color (Azul, Bordo, Gris, Negro, Purpura, Rojo, Verde) — álbum + portada animada + mobile-only + transición de salida.
- [x] Verificado en `/draft-moderno/[slug]` con foto real, en el navegador, mobile y responsive.
- [x] Verificado que cambiar `albumStyle` en la DB cambia el render real.
- [x] `?heroBlur=1&coverTint=1&tintColor1=...&tintColor2=...` era SOLO un override del Lab de pruebas (`src/app/draft-moderno-lab/`, `ModernoTemplateLab.tsx`) — no existe en real, el trigger real es tener `portadaImagenFondoDesktop` cargado. El Lab sigue en el repo, sin linkear, por si hace falta seguir prototipando ahí.

## Hallazgos que cambian el plan para las 21 familias restantes

1. **Las 22 familias (`TemplateTipo` en `template-preview-registry.tsx`) comparten la misma estructura de variables** (`isCoverOpen`, `heroBgMobile`, `AlbumCarousel photos={allPhotos} hideHeader`, etc.) — el wiring mecánico de la sección 8.3 de la guía aplica a todas.
2. **⚠️ Dos arquitecturas de color distintas conviven en el repo**:
   - **Hardcoded hex** (Moderno, Chic, Cine, Circuito, Corporate, Cristal3D, Editorial, Elegant, GardenParty, GoldenDusk, Holograma, Infantil, LoftIndustrial, Neon, Nordico, Onix, Riviera, BonVoyage): el JSX tiene `backgroundColor: '#HEX'` literal. Wiring directo, igual que Moderno.
   - **CSS custom properties** (LuzLuna, Petalos, Seda — probablemente la tanda "18 plantillas de `imple-masiva`" mencionada en la sección 7 de la guía): el JSX tiene `backgroundColor: 'var(--t-bg)'`, acento en `var(--t-acc)`. Para estas hay que resolver el hex real donde se DEFINE la variable (buscar el wrapper raíz que setea `--t-bg`/`--t-acc` inline) antes de poder pasarle `scrimColorRgb`/`tintColor1`/`tintColor2` a `AnimatedCoverPhoto` (que espera valores literales, no acepta `var()` anidado sin que la familia ya tenga una variante `-rgb` de esas variables, que hay que confirmar si existe).
3. **⚠️ ~10 de las 21 son de tema claro** (fondo claro, texto oscuro) — el texto de la portada necesita forzarse a un color claro cuando hay foto animada, si no queda ilegible sobre el scrim oscuro. Documentado en la guía, sección 8.3. Confirmado por archivo con `bg:` en la tabla de abajo; "?" = no confirmado todavía.
4. **El acento "de marca" de una familia puede no ser constante entre sus variantes de color** (pasó en Moderno con el esmeralda) — no asumir, revisar cada variante al propagar.

## Tabla de familias — colores relevados, tier propuesto, estado

Efecto según personalidad (criterio del asistente, sección 8.1 de la guía — **ajustar al verlo en vivo, no es definitivo hasta que se confirme visualmente**):

| Familia | Arquitectura | Tema | Acento principal | Acento 2° / bg | Efecto propuesto | Estado |
|---|---|---|---|---|---|---|
| **Moderno** | hex | oscuro | `#C9A876` | `#3E7A6A` (solo base) | `enfoque` | ✅ hecho y verificado |
| BonVoyage | hex | claro (`#F4F9FB`) | `#2E7EA6` (azul océano) | `#1B3A5C` | `enfoque` | pendiente |
| **Chic** | hex | claro (`#FBF3EA`) | `#C9A876` (dorado) | `#241E12` | `enfoque` | ✅ hecho y verificado (base + 6 variantes) |
| **Cine** | hex | oscuro (`#17130F`) | `#C08A3E` (ámbar cálido) | `#9C8F7A` | `enfoque` + tinte ámbar/teal (`#2E4A52`) | ✅ hecho y verificado (base + 4 variantes) |
| **Circuito** | hex | oscuro (`#08080A`) | `#39FFD0` / `#FF2E9B` (cian/magenta neón) | — | `flash` | ✅ hecho y verificado (base + 5 variantes) |
| **Corporate** | hex | oscuro (`#10131C`) — pero **1 de sus 4 variantes (Claro) es clara**, ver nota abajo | `#5C8DFF` (azul corporativo) | `#171B27` | `geometric`, sin tinte | ✅ hecho y verificado (base + 4 variantes) |
| **Cristal3D** | hex | oscuro (`#0A0E16`) | `#8FD3FF` / `#B9A6FF` (cian/violeta glass) | — | `shimmer` | ✅ hecho y verificado (base + 5 variantes) |
| Editorial | hex | claro (`#EDEBE5`) | `#A3123B` (vino editorial) | `#17140F` | `geometric` (tipografía/editorial = sobrio) | pendiente |
| Elegant | hex | claro (`#F9F7F1`) | `#C79A4B` (dorado clásico) | `#1A2B33` | `enfoque` | pendiente |
| GardenParty | hex | claro (`#FBF4EC`) | `#D97757` (terracota festivo) | `#3A2A22` | `shimmer` | pendiente |
| GoldenDusk | hex | claro (`#FDF6F0`) | `#C8956C` (atardecer) | `#3B2A2A` | `shimmer` | pendiente |
| **Holograma** | hex | oscuro (`#0D0D14`) | `#A78BFA` / `#22D3EE` (violeta/cian iridiscente) | — | `flash` | ✅ hecho y verificado (base + 5 variantes) |
| Infantil | hex | claro (`#FFF7F2`) | `#FF5C8A` / `#9B7FE8` (rosa/violeta) | — | `flash` (juguetón) o `bounce` si se prioriza el rebote sobre el flash | pendiente |
| JardinSeda | hex | claro (`#FCEFF1`) | `#B79FC4` (lavanda jardín) | `#3A2E33` | `shimmer` | pendiente |
| **LoftIndustrial** | hex | oscuro (`#121212`) — pero **1 de sus 4 variantes (Claro) es clara** | `#E0B84B` (ámbar industrial) | `#1C1C1C` | `geometric`, sin tinte | ✅ hecho y verificado (base + 4 variantes) |
| **LuzLuna** | CSS vars (valores literales, mismo wiring) | oscuro (`--t-bg: #171425`) — pero **2 de sus 4 variantes son claras** (Perlada, PerlaSuave) | `--t-acc: #B9A6D9` (lavanda luna) | — | `shimmer` | ✅ hecho y verificado (base + 4 variantes) |
| **Neon** | hex | oscuro (`#0D0D10`) | `#39FFD0` / `#FF2E9B` | — | `flash` | ✅ hecho y verificado (base + 5 variantes) |
| **Nordico** | hex | claro (`#FFFFFF`) | neutros (`#111111`/`#5B5850`, sin acento de color fuerte) | — | `geometric`, sin tinte | ✅ hecho y verificado (base + 4 variantes) |
| **Onix** | hex | oscuro (`#140B14`) | `#D89AA0` (rosa piedra preciosa) | `#B9A6B4` | `enfoque` | ✅ hecho y verificado (base + 5 variantes) |
| **Petalos** | CSS vars | **claro** (`--t-bg: #FFF1EF`) — pero **1 de sus 4 variantes (VinoVibrante) es oscura** | `--t-acc: #E23B4E` (rojo flor) | — | `shimmer` | ✅ hecho y verificado (base + 4 variantes) |
| **Riviera** | hex | claro (`#FAF1E4`) | `#C1734A` (terracota mediterráneo) | `#3E2E20` | `enfoque`, sin tinte | ✅ hecho y verificado (base + 4 variantes) |
| **Seda** | CSS vars | **claro** (`--t-bg: #FBF3EE`) — pero **2 de sus 4 variantes (Esmeralda, Nocturna) son oscuras** | `--t-acc: #C9A0A6` (rosa seda) | — | `enfoque` | ⚠️ wireado, typecheck OK, **falta verificar en navegador** |

**Nota Cine**: no hay un 5° `effect` de "grano/flicker de cine" implementado todavía en `AnimatedCoverPhoto.tsx` (los 4 que existen son `enfoque`/`shimmer`/`flash`/`geometric`, pensados para bodas/XV/eventos, no específicamente para Cine). Al llegar a esta familia, decidir si `enfoque` alcanza o si vale la pena sumar un 5° efecto (grano de película, ya prototipado y descartado en el Lab de Moderno por pedido del usuario en otro contexto — podría tener más sentido temático acá).

## Orden de trabajo sugerido

1. ~~Resolver arquitectura CSS-vars (LuzLuna, Petalos, Seda)~~ — resuelto: son valores hex literales asignados a `--t-acc`/`--t-bg` inline, mismo wiring que las familias hex, solo cambia de dónde se lee el color. No hace falta un patrón de wiring distinto en la guía.
2. Una familia de **tema claro** primero (Chic) para probar en vivo la solución de texto claro-sobre-foto (sección 8.3 de la guía) antes de repetirla en las otras ~10.
3. Resto de las familias de tema oscuro (son las más parecidas a Moderno, más rápidas).
4. Auditoría final: `grep -c "acp-mobile-only" src/components/templates/*Template*.tsx` — tiene que dar 1+ en las 132 archivos de familias reales (todo excepto `ConviteTemplate.tsx`, `DraftTemplate.tsx`, `CollaborativeAlbumModern.tsx`, que no son parte de `TemplateTipo` y no se tocan).
5. **Última tarea, recién con las 21 familias ya terminadas**: releer `docs/GUIA_TECNICA_PLANTILLAS.md` secciones 8-9 completas y actualizarlas con todos los matices/trampas nuevos que hayan aparecido familia por familia durante el rollout (ver el aviso en el bloque "ESTADO ACTUAL" arriba de este archivo). No hacerlo a mitad de camino, en una sola pasada al final — así la guía queda consistente en vez de parcheada de a poco.
6. **Recién después de todo lo anterior** (rollout de las 21 familias + actualización final de la guía): arrancar la feature "Save the Date", ver sección siguiente.

## Feature en curso — "Save the Date" arriba del countdown

Pedido explícito del usuario: la fecha que se mostraba en la invitación era "muy pequeña" — se agregó un bloque nuevo **arriba del countdown**, con: kicker "Save the date", la fecha en tamaño grande/protagonista, subtítulo día-de-semana+año, y un botón "Agregar al calendario". 100% aditivo (no reemplaza nada de lo que ya había).

### 🟡 ESTADO ACTUAL (parate acá si se retoma)

**Hecho:**
- Componente compartido `src/components/invitation/v2/SaveTheDate.tsx` (patrón `Album.tsx`/`AnimatedCoverPhoto.tsx`). Props: `eventName`, `targetDate`, `location?`, `description?`, `dark?` (legado, casi no se usa — ver nota de contraste abajo).
- **Botón "Agregar al calendario" cross-platform**: detecta iOS (`navigator.userAgent`) → genera un `.ics` (RFC5545) servido como `data:text/calendar` (abre la hoja nativa de Calendario en Safari iOS). Resto (Android/desktop) → link a `calendar.google.com/calendar/render?action=TEMPLATE...` (abre la app o la web de Google Calendar). Probado en real: el link de Google Calendar pre-completa nombre/fecha-hora/ubicación correctamente (verificado en el navegador, cerrado sin guardar el evento real). La rama iOS se verificó por lógica (el `.ics` generado es RFC5545 válido, escapeado correctamente) pero no en un dispositivo iOS real — no hay forma de probarlo en este entorno de desktop.
- **⚠️ Trampa real encontrada y ya resuelta, importante para cualquier familia que falte**: una auditoría de contraste WCAG (relative luminance) sobre los 128 archivos de plantilla mostró que **~45% de las combinaciones `--t-acc`/`--t-muted` vs `--t-bg` no llegan al mínimo legible** (4.5:1 texto chico, 3:1 texto grande) — no es un caso aislado de una familia, es extendido (BonVoyage, Chic, Editorial, GardenParty, GoldenDusk, Infantil, JardinSeda, LuzLuna, Petalos, Riviera, Seda, entre otras, tienen algún color por debajo del umbral). En vez de curar el color a mano por familia (inviable a esta escala), `SaveTheDate.tsx` lee el valor RESUELTO real de `--t-acc`/`--t-muted`/`--t-bg` en el navegador (`getComputedStyle` en un `useEffect`) y si no llega al contraste mínimo, lo mezcla algorítmicamente hacia negro o blanco (el que corresponda según si el fondo es claro u oscuro) hasta alcanzarlo — conserva el matiz de marca, solo lo ajusta lo necesario. Verificado con el peor caso real de la auditoría (Petalos-Pastel, acc original `#F0B8C6` con contraste 1.62:1 contra bg `#FFF8F9`) en navegador: el texto de la fecha se ve claramente legible en un tono rosa oscurecido, sin perder identidad de marca.
- Wireado y verificado en navegador (exit transition + legibilidad + botón funcionando): **Moderno** (base) y **Petalos** (base, en la variante Pastel específicamente, el peor caso de contraste).

**Pendiente:**
1. **Verificar que el wizard no se trabe con la sección nueva** (pedido explícito del usuario, todavía sin cerrar del todo): análisis de código ya dice que es seguro por construcción — `WizardLivePreview.tsx` nunca manda `section: "save-the-date"` como target de scroll (no hay ningún paso del wizard mapeado a esa sección, ver el switch de `stepLabel` ahí), y la sección se renderiza siempre (sin gate condicional), así que no puede disparar el mecanismo de fallback-por-elemento-no-encontrado que causó el freeze viejo del paso "Álbum" (ver sección 9 de la guía técnica). **Falta la verificación EN VIVO en el wizard real** — se llegó hasta el paso "Fotos de Portada y Fondo" (`StepHeroImages`), que exige subir una foto real de "Portada Invitación" para poder avanzar al paso siguiente (Información Básica/Countdown, que es donde el preview scrollea a la sección donde vive Save the Date) — subir un archivo real requiere el tool `file_upload` de Chrome (no cargado todavía) o hacerlo a mano. **Retomar desde acá**: cargar una foto cualquiera en "Portada Invitación" en `/dashboard/invitaciones/crear`, avanzar hasta el paso "Información Básica" o "Countdown", y confirmar que el preview de la derecha scrollea sin colgarse.
2. **Propagar a las 21 familias restantes** (base + variantes de color, mismo criterio de lote que el resto del rollout) — todavía no se tocó ninguna salvo Moderno y Petalos (solo su variante Pastel, falta el resto de las variantes de Petalos también).
3. Reusar el mismo patrón de propagación ya probado (grep de anchors `import.*Countdown`/`<Countdown`/`const fechaEvento`/`const { title, em }`/`const lugarNombre`/`const direccion`, insertar `<SaveTheDate eventName={...} targetDate={fechaEvento} location={...} />` inmediatamente antes de cada `<Countdown ...>`) — el diseño NO necesita curaduría de color por familia (a diferencia de `AnimatedCoverPhoto`), el componente ya se adapta solo vía CSS vars + corrección de contraste automática.

**Decisiones de diseño ya confirmadas por el usuario en esta sesión** (no volver a preguntar):
- Sin recuadro/card con borde — el contenido flota directo sobre el fondo de la sección (se sacó un diseño inicial con card, el usuario pidió sacarlo).
- Botón "Agregar al calendario" chico/discreto, texto-link con ícono, no un botón grande con relleno (se suavizó desde un diseño inicial más prominente).
- Debe funcionar en Android e iOS (resuelto con el mecanismo dual descrito arriba).
- Debe respetar automáticamente la gama de colores de cada familia/variante, no verse roto por bajo contraste (resuelto con la corrección algorítmica de contraste).

## Feature en cola — Iconos animados Lordicon por sección (sin empezar)

Pedido explícito del usuario, textual: instalar y usar la librería **Lordicon** para poner íconos animados afines a cada sección de la invitación (ejemplo dado por el usuario: en la sección "Save the date", un calendario animado). Pidió explícitamente usar **"lo mejorcito que tenga gratis"** — o sea, dentro del tier gratuito de Lordicon, elegir los íconos de mejor calidad disponibles (no cualquiera solo por ser gratis).

**Sin empezar todavía — nada de lo siguiente está investigado ni decidido**:
- Lordicon se distribuye como archivos `.json` (Lottie-like, formato propio) + su propio web component `<lord-icon>` (paquete npm `lordicon` o CDN) — hay que revisar la licencia del tier gratis exacta (cuántos íconos, si permite uso comercial, atribución requerida) antes de instalar nada.
- No se relevó el catálogo gratuito para elegir un ícono por sección/familia (calendario para Save the Date, sobre/carta para RSVP, mapa/pin para ubicación, regalo para lista de regalos, etc.) — hay que buscar en el sitio de Lordicon cuáles están en el tier free y curarlos.
- No se decidió cómo integrarlo con Next.js (web component + SSR — probablemente necesita `dynamic import` con `ssr: false` o cargar el script solo client-side) ni dónde vive el wrapper compartido (¿`src/components/ui/LordIcon.tsx`?).
- No se decidió trigger de animación por ícono (`hover`, `click`, `loop`, `in-viewport`) — necesita curaduría por sección, no un valor genérico para todos.
- Depende de la feature "Save the Date" de arriba (el primer caso de uso mencionado por el usuario es el calendario animado ahí), así que como mínimo arranca después de esa, y realistamente después del rollout álbum/portada completo — mismo orden de prioridad que las demás features en cola.

## Feature en cola — Live preview del wizard: mostrar la portada de bienvenida en el paso "Portada" y el tope del cuerpo en "Plantilla" (evaluada, sin empezar)

Pedido del usuario, con una condición explícita: **"en caso que se pueda, agregar al plan"** — o sea, evaluar factibilidad primero, no implementar todavía. El pedido: en el wizard, al configurar el paso "Portada" (`StepHeroImages.tsx`, foto de portada de bienvenida + fondo), el live preview debería mostrar la portada de bienvenida animada (el overlay con nombre/dress code/botón). Al hacer clic en "Siguiente" y llegar al paso "Plantilla" (`StepDesign.tsx`), el preview debería mostrar el tope del CUERPO de la invitación (la sección hero con la foto principal, portada ya "abierta"), y de ahí en más seguir bajando con cada paso como ya hace. **Condición explícita del usuario, cita textual**: "es INNEGOCIABLE que ... no se tilde ... y baje normalmente acompañando los pasos del wizard, si técnicamente no se puede hacer esto. Desestimar" — o sea, si no se puede garantizar sin riesgo de que el preview se cuelgue/tilde, no vale la pena implementarlo.

**Evaluación de factibilidad (hecha, no implementado)**:
- Mecanismo actual (`WizardLivePreview.tsx` → `postMessage({type:"wizard-scroll-to", section})` → `preview-plantilla/page.tsx` los recibe y hace `window.scrollTo`): los pasos "Portada" Y "Plantilla" **ya mapean al mismo `section = "hero"`** (línea ~125 de `WizardLivePreview.tsx`), y ese caso especial simplemente hace `window.scrollTo({top:0})` — no distingue entre "portada cerrada" (overlay `position:fixed` full-viewport controlado por el state `isCoverOpen` DENTRO de cada `XxxTemplate.tsx`) y "portada abierta, scrolleado al tope del cuerpo".
- **El scroll de página no controla el overlay de portada** — `isCoverOpen` es un `useState` local de cada componente de plantilla, que solo cambia a `true` mediante el click en "Abrir invitación" (`openInvitation` → `isClosingCover` → `setTimeout` de 700ms → `setIsCoverOpen(true)`). Hoy no existe ningún mecanismo para que el iframe del preview le diga "abrí la portada" a la plantilla desde afuera.
- **Para implementarlo de verdad, hace falta agregar un mecanismo nuevo transversal a las 22 familias** (mismo alcance que el rollout de álbum/portada, no un ajuste chico): un prop o listener nuevo en cada `XxxTemplate.tsx` (ej. escuchar un `postMessage` tipo `"wizard-open-cover"` y llamar a `setIsCoverOpen(true)` directamente, saltando la animación de cierre) + diferenciar en `WizardLivePreview.tsx` los steps "Portada" (mandar `section:"hero"` sin abrir) vs "Plantilla" (mandar `section:"hero"` + un mensaje adicional para abrir la portada).
- **No es una feature de "sí o no" simple** — es técnicamente posible, pero el costo real es re-tocar las 22 familias (base + variantes) una vez más, agregando un listener de `postMessage` + una función que fuerce `isCoverOpen=true` sin depender del click del usuario. Dado el pedido explícito de "desestimar si no es trivial", **queda en el plan pero NO se arranca sin que el usuario confirme que quiere pagar ese costo** (alcance similar al de todo un rollout, no un fix chico) — no calza con la lectura de "si no se puede fácil, dejarlo".

**Prioridad confirmada por el usuario**: esto (junto con Lordicon, sección anterior) **no es prioritario** — queda anotado para más adelante, no bloquea nada del orden de trabajo actual. Orden confirmado: 1) terminar el rollout de fondos PNG (en curso), 2) "Save the Date", 3) recién después, si el usuario lo pide, esto y/o Lordicon.

## Feature en cola — Plantillas temáticas (personajes, equipos de fútbol, películas, etc.) sobre la base de Moderno (anotada, sin empezar)

Pedido del usuario, para **después de terminar "Save the Date"**. Idea: nuevas variantes de plantilla, tomando `ModernoTemplate.tsx` como base estructural, pensadas para temáticas con fondo propio (personaje, equipo de fútbol, película, etc. — el usuario dará los assets de fondo más adelante, todavía no entregó ninguno). Reglas explícitas del usuario, cita/paráfrasis textual:

- **El fondo temático SIEMPRE se muestra en la portada de bienvenida, incluso si el usuario cargó su propia foto en "Portada de bienvenida"** — a diferencia de todo lo demás en este rollout (donde la foto real del usuario tiene prioridad y el fondo/efecto es solo un fallback), en estas plantillas temáticas el fondo es fijo y gana siempre por diseño. Este es el primer caso en toda la app de una plantilla donde la foto de portada del usuario NO se respeta — hay que tenerlo muy claro para no tratarlo con el mismo mecanismo (`portadaFondoAnimado`/`CoverFallbackBg`) que el resto, que sí depende de si hay foto o no.
- **La paleta de color de TODA la plantilla (no solo la portada) la dicta el fondo temático elegido**, no al revés. Ejemplos dados por el usuario: fondo de Banfield (equipo de fútbol) → plantilla en verde y blanco; fondo de Spiderman → plantilla en azul y rojo. Cada temática necesita su propia curaduría de paleta (como ya se hizo con `effect`/tinte por familia en la sección 8.4 de la guía técnica), no una paleta genérica.
- **Legibilidad**: el usuario pidió explícitamente prestar atención a esto — con fondos muy cargados/oscuros (ej. Spiderman) el texto debería ir en blanco; criterio caso por caso, no una regla fija.
- **Alcance de tipo de evento**: el usuario aclaró que estas plantillas temáticas van sobre todo a XV y a Eventos (cumpleaños de cualquier edad, infantiles, etc.) — no bodas.
- **Nada de esto está diseñado todavía** — falta: cuántas temáticas arrancan, de dónde vienen los assets (el usuario los va a proveer), si cada temática es una plantilla nueva en `TemplateTipo` o una variante de color más de una familia "Moderno-temáticas", y cómo se relaciona con el mecanismo de portada existente dado que rompe la regla "la foto del usuario manda" que aplica a las demás 22 familias.
