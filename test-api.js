const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const inv = await prisma.invitation.findFirst({ orderBy: { updatedAt: 'desc' } }); console.log('BEFORE:', inv.templateTipo); await prisma.invitation.update({ where: { id: inv.id }, data: { templateTipo: 'MODERNO' } }); const invAfter = await prisma.invitation.findFirst({ orderBy: { updatedAt: 'desc' } }); console.log('AFTER:', invAfter.templateTipo); } main().finally(() => prisma.$disconnect());
